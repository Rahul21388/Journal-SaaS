// FILE: lib/claude.ts
import type { Entry } from './firestore'

// Full Claude implementation coming in Day 5.
export async function generateDigest(_entries: Entry[]): Promise<string> {
  return 'AI digest coming in Day 5.'
}
